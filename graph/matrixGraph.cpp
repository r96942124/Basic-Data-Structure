#include"matrixGraph.h"

void matrixGraph::insertVertex(int vertex)
{
  int index;
  if (indexMap.end()==indexMap.find(vertex)){
     if (!indexUnused.empty()){
        
     }
     std::cout<<"Insert Vertex: "<<vertex<<std::endl;
  }
}
